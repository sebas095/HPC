cmake_minimum_required(VERSION 2.8)
project(sobelFilter)
find_package(OpenCV REQUIRED)
find_package(CUDA REQUIRED)
cuda_add_executable(sobelFilter sobelFilter.cu)
target_link_libraries(sobelFilter ${OpenCV_LIBS})
